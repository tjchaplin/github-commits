#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-travis/bin/grunt-travis" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-travis/bin/grunt-travis" "$@"
  ret=$?
fi
exit $ret
